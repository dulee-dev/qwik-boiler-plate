import { css } from '~/styled-system/css';

export const s = {
  submit: css({
    w: 'full',
    display: 'flex',
    ai: 'center',
    jc: 'center',
    minH: '3rem',
  }),

  loading: css({
    animationStyle: 'infinit-spin',
  }),
};
