import { css } from '~/styled-system/css';

export const s = {
  wrapper: css({
    dp: 'flex',
    ai: 'center',
  }),

  inputBox: css({
    dp: 'flex',
    ai: 'center',
    mr: '0.5rem',
  }),

  radio: css({
    mr: '0.25rem',
  }),

  text: css({
    borderBottom: '1px solid black',
  }),
};
